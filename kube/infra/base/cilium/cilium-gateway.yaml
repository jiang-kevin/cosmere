apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gw-private
spec:
  gatewayClassName: cilium
  listeners:
  - protocol: HTTP
    port: 80
    name: http-gw
    allowedRoutes:
      namespaces:
        from: All